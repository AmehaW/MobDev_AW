<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobDev_AW.StudentDetails"
             Title="StudentDetails">
    <StackLayout>
        <Frame BackgroundColor="#F5F5DC" Padding="10" Margin="20">
            <StackLayout>
                <!-- Display Student Details Here -->
                <Label Text="{Binding GivenName}" />
                <Label Text="{Binding FamilyName}" />
                <Label Text="{Binding StudentNumber}" />
                <Label Text="{Binding EnrollmentDate}" />

                <Label Text="{Binding Phone}" />
                <Label Text="{Binding Department}" />
                <Label Text="{Binding Street}" />
                <Label Text="{Binding City}" />
                <Label Text="{Binding State}" />
                <Label Text="{Binding ZipCode}" />
                
                <!-- Add any other details you want to display -->
            </StackLayout>
        </Frame>
    </StackLayout>
</ContentPage>